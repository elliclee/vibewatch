# 安全与隐私

VibeWatch 是单用户自部署项目。上传密钥保存在 Mac 的权限 600 私有配置和 Worker secret 中；客户端仅持有只读设备令牌，Apple 端凭据存入 Keychain。

云端保存设备 token / 一次性配对码的 SHA-256 哈希及最新额度快照。配对码有效 10 分钟；只读 token 在撤销前有效。不要分享配对页、链接或 config.json。

可选本机用量只上传计数汇总、时间和受限模型标签，不上传对话、会话标识、文件路径或 Codex 认证文件。统计会覆盖该 Mac 可读取的本地会话，不能用于精确的账号隔离计费。

## 报告漏洞

请使用仓库 Security → Report a vulnerability 私下提交问题，不要在公开 Issue 中放入可用凭据或未脱敏的日志。如果该入口不可用，可提交不含细节的 Issue 请求私密联系渠道。

若自己的密钥泄露，轮换 Worker 上传密钥并更新 Mac 配置；设备令牌需要通过 `revoke` 单独撤销。删除本地配对不等于立即撤销离线手表的云端身份。

本仓库不包含托管服务承诺、开发者证书或任何人的 Codex 登录凭据。
