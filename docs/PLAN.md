# 项目状态与路线图

## 当前基线

0.1.0，内部验证构建 13。默认四目标：iPhone App、手机小组件、Watch App、Watch 表盘组件。已完成真实额度采集、Workers/D1 同步、一次性配对、Watch 两页布局、本机用量统计及离线状态。已有内部 TestFlight 分发验证，但开源使用者需要自己的云端与签名账号。

## 开源发布整理

- MIT 许可，中文 README、部署、构建、架构与贡献说明。
- 移除个人部署 ID、认证默认值、历史操作记录；私有材料留在忽略目录。
- 提供个人 XcodeGen 配置生成器、可配置的四目标签名脚本。
- 干净检出验证 Python、云端、Swift 模型和 Apple SDK 构建，添加 CI。

## 后续工作

- iPhone 桌面/锁屏小组件：已纳入默认构建，继续验证真实升级、添加组件和系统刷新调度。
- 验证不同 Watch 尺寸、动态字号、Always On、减少动态效果与长时间系统组件调度。
- 验证 Mac 休眠唤醒、网络中断、撤销设备与切换账号的长时间行为。
- 扩充 Codex 接口变化的兼容性测试。

## 工程原则

业务代码独立实现，不引入许可不兼容的第三方源码或素材。缺失数据显示未知，不推断无限或满额；没有明文凭据兜底。复杂开发先补简短方案，新增关键行为测试，完成后同步公开运行说明。

本项目暂不计划在第一版引入多租户、APNs 推送、自定义系统表盘或通知系统。

## build 9 手机小组件发布

已完成：四目标主工程、四份描述文件与归档/导出签名验证，6 项客户端测试及 6 项配置/签名测试通过。0.1.0（9）已为 VALID / IN_BETA_TESTING，内部组访问已确认。保留旧私有 Keychain 权限用于升级迁移；开源构建文档已同步。

## 手机组件视觉优化

对齐 Watch 看板：桌面小号双行分段额度、中号双列大数字与重置卡片；共享锁屏矩形使用分段条。保留深浅色、单窗口、未知/过期/不可用语义，不改同步与刷新策略。使用真实展示源码生成多状态预览并完成四目标编译验证。

视觉优化已实现，深浅色源码预览已检查，四目标真机 SDK 无签名编译通过；0.1.0（10）已正式签名发布，Apple 状态 VALID / IN_BETA_TESTING，内部组访问已确认。

## 组件今日用量

桌面小号/中号和手机锁屏矩形增加本机 token 汇总；缺失不当作 0，部分与跨日明确标注。时间线增加统计日边界，模型测试覆盖切换。已检查源码布局预览，9 项模型测试通过；四目标正式签名归档与导出校验完成。0.1.0（11）已发布内部 TestFlight，Apple 状态 VALID / IN_BETA_TESTING，内部组访问及中文测试说明已确认。

## App 内视觉统一（build 12）

iPhone 首页沿用 Watch 的分段额度条、窗口绿蓝配色、低额度提示与描边重置卡片；使用手机动态字号，保留单窗口、缺失与过期语义。增加同口径的本机今日用量、小时分布和输入／输出／缓存明细；配置收进展开区，保持刷新、配对、断开流程。已完成合成数据深浅色、320pt 窄屏、单窗口、低额度、过期与缺失状态源码预览，以及四目标 SDK 编译验证。采集状态置于看板上方，优先呈现过期提示。0.1.0（12）已正式签名发布内部 TestFlight，Apple 状态 VALID / IN_BETA_TESTING，内部组访问和中文测试说明已确认。

## App 图标更新（build 13）

iPhone 与 Watch 使用统一的蓝紫流动曲面与浅色背景图标（用户选定的 A 方案）。母版保存在 design/，资源脚本从母版生成无透明通道的 1024 × 1024 PNG，避免恢复旧设计。

0.1.0（13）已完成四目标签名归档、导出校验和内部 TestFlight 发布，Apple 状态 VALID / IN_BETA_TESTING，内部组访问及中文测试说明已确认。
