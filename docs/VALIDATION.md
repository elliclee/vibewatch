# 验证范围

## iPhone 看板视觉统一（待发布）

2026-09-24：四目标 Release 真机 SDK 无签名编译通过。使用 scripts/render_phone_dashboard.swift 检查深浅色、单周窗口、320pt 窄屏低额度、过期与缺失状态；预览只用合成数据，不代表实机截图。未更改配对、网络或用量统计逻辑，原有扫码、刷新、额度选择、同步手表与断开确认仍保留。动态字号和实际设备交互需真机补充验证。

## 0.1.0（11）组件今日 tokens

9 项 Swift 模型测试通过，覆盖统计日边界切换；深浅色多状态源码预览已检查。四目标真机 SDK 编译、正式签名归档和导出校验通过。Apple 状态 VALID / IN_BETA_TESTING，内部组访问及中文测试说明已确认。桌面小号／中号和锁屏矩形显示本机今日用量，实际系统布局及跨日刷新仍需设备验证。

## 0.1.0（10）组件排版更新

深浅色多状态源码预览已检查，四目标真机 SDK 编译、正式签名归档与导出校验通过。Apple 状态 VALID / IN_BETA_TESTING，内部组访问及中文测试说明已确认；实际组件展示仍需设备验证。

## 0.1.0（9）手机小组件

Apple 状态 VALID / IN_BETA_TESTING，内部组访问已核实，中文测试说明已填写。

2026-09-23：默认工程启用四目标，iPhone 使用共享 Keychain 和 App Group。四个 App Store 描述文件已验证包含所需组，正式签名归档及导出后的四个包均通过校验；手机主 App 保留旧私有 Keychain 权限。

6 项配置/签名测试、6 项 iOS 客户端测试通过，覆盖旧私有配对迁移、已有共享配对优先、断开后不恢复旧凭据、缓存隔离和 API 行为。1 项旧私有模式专用测试按配置跳过，独立 HTTP 测试未运行。桌面/锁屏实际添加、真实升级与系统刷新调度仍待用户设备验证。

## 0.1.0（8）内部更新

2026-09-23：统一额度名称为 Codex。9 项模型测试通过，三目标正式签名归档及导出后验证通过，Apple 已处理为 VALID / IN_BETA_TESTING，并确认内部组访问。手机小组件仍未加入默认构建。

## 历史功能基线

内部构建 0.1.0（7）已完成三目标分发签名、归档、导出及 TestFlight 处理验证；用户确认过 iPhone → Watch 配置同步和真实额度读取。

- 采集器：14 项测试，覆盖归一化、窗口缺失、计数去重、跨日基线与异常记录。
- Workers：7 项测试，覆盖鉴权、一次性兑换、快照排序、撤销与用量白名单。
- Swift 模型：9 项测试，覆盖窗口、过期状态、旧协议兼容与大整数计数。
- 默认 iPhone 客户端：5 项通过；2 项未来手机共享迁移测试按配置跳过。HTTP 集成测试使用独立 scheme。
- 三目标真机 SDK Release 归档与签名包嵌套结构验证通过。
- 源码预览检查过正常/低额度/用量页；它不能替代 watchOS 实机截图。

## 尚需设备验证

不同 Watch 屏幕、动态字号、Always On、离线恢复、WidgetKit 长时间刷新预算、Mac 休眠唤醒，以及最低 iOS 17 / watchOS 10 的全面兼容性。构建通过不等于上述行为全部验收。

## 开源构建

CI 使用合成数据，不需要生产密钥。2026-09-23 开源准备已在独立的干净源码目录验证：14 项采集器测试、7 项云端测试与 TypeScript 类型检查、9 项 Swift 模型测试、默认三目标真机 SDK 无签名编译通过；另有 5 项配置/签名校验测试通过，自定义 Bundle 前缀与 Team 的 Xcode 设置验证通过。复现命令见 [DEVELOPMENT.md](DEVELOPMENT.md)，具体持续结果见仓库 Actions。
